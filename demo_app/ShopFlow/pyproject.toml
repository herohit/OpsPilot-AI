[project]
name = "shopflow"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "herohit", email = "rohitnegi323@gmail.com" }
]
requires-python = ">=3.14"
dependencies = [
    "fastapi[standard]>=0.141.1",
    "sqlalchemy>=2.0.54",
]

[project.scripts]
shopflow = "shopflow:main"

[build-system]
requires = ["uv_build>=0.12.9,<0.13.0"]
build-backend = "uv_build"
